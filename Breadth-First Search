from collections import deque

def bfs(graph, start):
    visited = set()
    queue = deque([start])
    order = []
    while queue:
        v = queue.popleft()
        if v not in visited:
            visited.add(v)
            order.append(v)
            for u in graph[v]:
                if u not in visited:
                    queue.append(u)
    return order

graph = {
    "A": ["B", "C"],
    "B": ["D", "E"],
    "C": ["F"],
    "D": [],
    "E": [],
    "F": []
}

print(bfs(graph, "A"))
